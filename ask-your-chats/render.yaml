services:
  - type: web
    name: ask-your-chats
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false
      - key: HF_API_KEY
        sync: false
