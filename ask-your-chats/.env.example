# Copy to .env and fill in. .env is gitignored.
GROQ_API_KEY=gsk_...         # console.groq.com — free
HF_API_KEY=hf_...            # huggingface.co/settings/tokens — free (Read token is enough)

# Optional overrides
PORT=3000
CHAT_MODEL=llama-3.3-70b-versatile
