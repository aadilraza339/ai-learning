diff --git a/src/routes/separation/nocFormsUnified.ts b/src/routes/separation/nocFormsUnified.ts
index b957e49e..e10e3d91 100644
--- a/src/routes/separation/nocFormsUnified.ts
+++ b/src/routes/separation/nocFormsUnified.ts
@@ -62,6 +62,7 @@ async function maybeGenerateRelievingLetter(opts: {
   userId: string
   tenant: string
   log: FastifyRequest["log"]
+  forceNotification?: boolean
 }): Promise<boolean> {
   if (!opts.shouldTrigger) return false
 
@@ -82,6 +83,7 @@ async function maybeGenerateRelievingLetter(opts: {
     separationId: opts.separationRequestId,
     userId: opts.userId,
     tenant: opts.tenant,
+    forceNotification: opts.forceNotification,
   })
 
   if (!result.success) {
@@ -340,6 +342,7 @@ export default async function nocFormsUnifiedRoutes(fastify: FastifyInstance) {
         userId: request.user.userId,
         tenant: request.user.tenant,
         log: request.log,
+        forceNotification: body.status === "NOC_STATUS.COMPLETED" && type === "hropsPreLwd" && region === "UK",
       })
 
       return { success: true, data: result, relievingLetterGenerated }
@@ -437,6 +440,7 @@ export default async function nocFormsUnifiedRoutes(fastify: FastifyInstance) {
         userId: request.user.userId,
         tenant: request.user.tenant,
         log: request.log,
+        forceNotification: body.status === "NOC_STATUS.COMPLETED" && type === "hropsPreLwd" && region === "UK",
       })
 
       return { success: true, data: result, relievingLetterGenerated }
diff --git a/src/services/separation/generateRelievingLetter.ts b/src/services/separation/generateRelievingLetter.ts
index 5e51d9d1..f5b61e31 100644
--- a/src/services/separation/generateRelievingLetter.ts
+++ b/src/services/separation/generateRelievingLetter.ts
@@ -35,6 +35,7 @@ interface GenerateRelievingLetterParams {
   actualLwd?: string
   skipDbInsert?: boolean // Set to true when regenerating (don't insert new record)
   skipNotification?: boolean // Set to true when regenerating (caller sends its own notification)
+  forceNotification?: boolean // Set to true to notify even for involuntary separations (e.g. UK hropsPreLwd)
 }
 
 interface GenerateRelievingLetterResult {
@@ -66,6 +67,7 @@ export async function generateRelievingLetter({
   actualLwd,
   skipDbInsert = false, // Default to false for initial generation
   skipNotification = false, // Default to false for initial generation
+  forceNotification = false,
 }: GenerateRelievingLetterParams): Promise<GenerateRelievingLetterResult> {
   try {
     // Query separation details with related employee information
@@ -243,7 +245,7 @@ export async function generateRelievingLetter({
     // Involuntary: generate the letter (above) but never notify the employee.
     // Skip notification when called from regeneration — the caller sends its own
     // LWD change notification instead.
-    if (!skipNotification && details.separationType === SEPARATION_CATEGORY.VOLUNTARY) {
+    if (!skipNotification && (forceNotification || details.separationType === SEPARATION_CATEGORY.VOLUNTARY)) {
       await sendNotification({
         fastify,
         separationId: separationId,
