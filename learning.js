var asteroidCollision = function (asteroids) {
    let output = []
    for (let i = 0; i < asteroids.length; i++) {
        let n = asteroids[i]


    }

    return output
    // if(0 > n)
    // // loop on temp
    // if(tempPostive[-1] > Math.abs(n)){
    //     output.push(tempPostive[-1])
    // }
    // else{
    //     output.push(n)
    // }
};

console.log(asteroidCollision([5, 10, -5])) //[5,10]